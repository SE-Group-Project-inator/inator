package com.example.inator;

public class Square extends Shape{
    double length;

    Square(int x, int y, double length) {
        this.x = x;
        this.y = y;
        this.length = length;
    }
}
